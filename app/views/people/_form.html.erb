<%= form_with(model: person, local: true) do |form| %>
  <% if person.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(person.errors.count, "error") %> prohibited this person from being saved:</h2>

      <ul>
      <% person.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :nome %>
    <%= form.text_field :nome %>
  </div>

  <div class="field">
    <%= form.label :email %>
    <%= form.text_field :email %>
  </div>

  <div class="field">
    <%= form.label :lattes %>
    <%= form.text_field :lattes %>
  </div>

  <div class="field">
    <%= form.label :Titulação %>
    <%= form.select :titulacao, ['Estudante','Graduando', 'Graduado', 'Mestrando', 'Mestre', 'Doutorando', 'Doutor'] %>
  </div>

  <div class="field">
    <%= form.label :funcao %>
    <%= form.select :funcao, ['Pesquisador', 'Coordenador', 'Desenvolvedor'] %>
  </div>


   <div class="field">
      <%= form.label :imagem %>
      <%= form.text_field :imagem %>
    </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
