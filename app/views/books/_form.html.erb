<%= form_with(model: book, local: true) do |form| %>
  <% if book.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(book.errors.count, "error") %> prohibited this book from being saved:</h2>

      <ul>
      <% book.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :titulo %>
    <%= form.text_field :titulo %>
  </div>

  <div class="field">
    <%= form.label :resumo %>
    <%= form.text_field :resumo %>
  </div>

  <div class="field">
    <%= form.label :paginas %>
    <%= form.text_field :paginas %>
  </div>

  <div class="field">
    <%= form.label :tipo %>
    <%= form.text_field :tipo %>
  </div>

  <div class="field">
    <%= form.label :idioma %>
    <%= form.text_field :idioma %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
